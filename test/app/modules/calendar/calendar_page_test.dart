import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:sully/app/modules/calendar/calendar_page.dart';

void main() {
  testWidgets('CalendarPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CalendarPage(title: 'Calendar')));
    //  final titleFinder = find.text('Calendar');
    //  expect(titleFinder, findsOneWidget);
  });
}
