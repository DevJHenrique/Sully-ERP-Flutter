import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:sully/app/modules/calendar/calendar_controller.dart';
import 'package:sully/app/modules/calendar/calendar_module.dart';

void main() {
  initModule(CalendarModule());
  // CalendarController calendar;
  //
  setUp(() {
    //     calendar = CalendarModule.to.get<CalendarController>();
  });

  group('CalendarController Test', () {
    //   test("First Test", () {
    //     expect(calendar, isInstanceOf<CalendarController>());
    //   });

    //   test("Set Value", () {
    //     expect(calendar.value, equals(0));
    //     calendar.increment();
    //     expect(calendar.value, equals(1));
    //   });
  });
}
