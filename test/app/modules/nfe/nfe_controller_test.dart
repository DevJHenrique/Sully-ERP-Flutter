import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:sully/app/modules/nfe/nfe_module.dart';

void main() {
  initModule(NfeModule());
  // NfeController nfe;
  //
  setUp(() {
    //     nfe = NfeModule.to.get<NfeController>();
  });

  group('NfeController Test', () {
    //   test("First Test", () {
    //     expect(nfe, isInstanceOf<NfeController>());
    //   });

    //   test("Set Value", () {
    //     expect(nfe.value, equals(0));
    //     nfe.increment();
    //     expect(nfe.value, equals(1));
    //   });
  });
}
